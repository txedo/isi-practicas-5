\section{Estadísticas de funcionamiento} \label{estadisticas}

Las pruebas se han realizado en un ordenador con procesador Intel Centrino a 2.0 GHz y memoria RAM de 2.0 GB. 

Utilizando una caché de tamaño 1, el tiempo que se ha obtenido en la indexación de los 239 documentos es .... Además, se observa que el uso de CPU, así como del acceso a disco, se mantiene constante durante todo ese tiempo.

Utilizando una caché de tamaño 1000, el tiempo que se ha obtenido en la indexación de los 239 documentos es .... En este caso, el acceso a disco viene dado por intervalos, que coinciden con la sincronización de las cachés con las bases de datos.